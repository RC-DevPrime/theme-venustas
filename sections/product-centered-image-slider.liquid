{% style %}
    .product-centered-image-slider-section{
        background-color: #1C1C1C;
    }
    .centered-image-item{
        width:100%;
        height:100%;
    }
    .centered-image-item img{
        width:100%;
        height:100%;
        object-fit:cover;
    }
    
    
    .product-centered-image-slider-section .float-nav button{
        background: #595959;
        position:unset;
        transform: unset;
    }
    .product-centered-image-slider-section .float-nav button:hover{
        background: #B93205;
    }
    .product-centered-image-slider-section .float-nav button i{
        color: #B93205;
    }
    .product-centered-image-slider-section .float-nav button:hover i{
        color: #FFF;
    }
    .product-centered-image-slider-section .pagination-navigations{
        position:relative;
    }
    .product-centered-image-slider-section .pagination-navigations .float-nav{
        position:unset;
        transform: unset;
        max-width:unset;
    }
    .product-centered-image-slider-section .swiper-pagination{
        background-color: unset;
        padding: 15px 0.6rem;
        border-radius: 50px;
        display: flex;
        margin: 0;
        position:unset;
        width: fit-content;
    }
    .product-centered-image-slider-section .swiper-pagination .swiper-pagination-bullet{
        background: #595959;
        opacity:1;
        margin:0px 4px;
        width:12px;
        height:6px;
        border-radius:4px;
    }
    .product-centered-image-slider-section .swiper-pagination .swiper-pagination-bullet.swiper-pagination-bullet-active{
        background: #B93205;
        width:55px;
    }
{% endstyle %}
<div class=" 
    {% if section.settings.nav_title != blank %} section-scroll
    {% else %}
      {% if section.settings.enabled_section_child_scroll %} section-scroll
       {% endif -%}
    {% endif %}" 

    {% if section.settings.nav_title != blank %}
        data-scroll="{{ section.settings.nav_title | handleize }}"
    {% endif %} 
    {% if section.settings.enabled_section_child_scroll %}
        data-child-scroll="{{ section.settings.section_scroll_nav | handleize }}"
    {% endif %}>

    <div class="container section-content pb-0">
        <div class="col-lg-9 col-md-9 col-sm-12 mx-auto">
            <h2 class="pp-bold title-sm f-white text-center pb-lg-4 pb-md-4 pb-sm-3">
                {{ section.settings.heading }}
            </h2>
            <div class="pp-regular pretitle f-white text-center pb-2">
                <span class="pretitle d-lg-block d-md-block d-sm-none">
                    {{ section.settings.description }}
                </span>
                <span class="body d-lg-none d-md-none d-sm-block">
                    {{ section.settings.description }}
                </span>
            </div>
        </div>
    </div>
    <div class="section-content px-0 pt-lg-5 pt-md-5 pt-sm-4">
        <div class="centered-image-slider position-relative">
            <div class="swiper-wrapper">
                {% for block in section.blocks %}
                    <div class="swiper-slide">
                        <div class="centered-image-item">
                            <img
                                src="{{ block.settings.image | img_url: '400x400' | image_url: 'format:auto' }}"
                                srcset="
                                    {{ block.settings.image | img_url: '400x400' | image_url: 'format:auto' }} 400w,
                                    {{ block.settings.image | img_url: '800x800' | image_url: 'format:auto' }} 800w,
                                    {{ block.settings.image | img_url: '1200x1200' | image_url: 'format:auto' }} 1200w,
                                    {{ block.settings.image | img_url: '1400x1400' | image_url: 'format:auto' }} 1400w"
                                sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw"
                                width="100%"
                                height="100%"
                                alt="{{ block.settings.image.alt | escape }}"
                                loading="lazy"
                                decoding="async"
                            />

                        </div>
                    </div>    
                {% endfor %}  
                {% for block in section.blocks %}
                    <div class="swiper-slide duplicate-slide">
                        <div class="centered-image-item">
                            <img
                                src="{{ block.settings.image | img_url: '400x400' | image_url: 'format:auto' }}"
                                srcset="
                                    {{ block.settings.image | img_url: '400x400' | image_url: 'format:auto' }} 400w,
                                    {{ block.settings.image | img_url: '800x800' | image_url: 'format:auto' }} 800w,
                                    {{ block.settings.image | img_url: '1200x1200' | image_url: 'format:auto' }} 1200w,
                                    {{ block.settings.image | img_url: '1400x1400' | image_url: 'format:auto' }} 1400w"
                                sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw"
                                width="100%"
                                height="100%"
                                alt="{{ block.settings.image.alt | escape }}"
                                loading="lazy"
                                decoding="async"
                            />

                        </div>
                    </div>    
                {% endfor %}  
            </div>
            <div class="pagination-navigations mt-lg-5 mt-md-5 mt-sm-4">
                <div class="float-nav d-flex align-items-center justify-content-center gap-lg-4 gap-md-4 gap-sm-2">
                    <button type="button" class="owl-prev" aria-label="Previous slide" disabled>
                        <i class="fa-solid fa-chevron-left f-white"></i>
                    </button>
                    <div class="swiper-pagination"></div>
                    <button type="button" class="owl-next" aria-label="Next slide" disabled>
                        <i class="fa-solid fa-chevron-right f-white"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
{% schema %}
{
    "class": "product-centered-image-slider-section overflow-hidden",
    "name": "Centered Image Slider",
    "limit": 1,
    "templates": ["product"],
    "settings": [
        {
            "type": "checkbox",
            "id": "enabled_section_child_scroll",
            "label": "Enabled Section Child Scroll"
        },
        {
            "type": "text",
            "id": "section_scroll_nav",
            "label": "Section Scroll Nav",
            "info": "Need to Enabled first Section Scroll"
        },
        {
            "type": "text",
            "id": "nav_title",
            "label": "Navigation Title"
        },
        {
            "type": "text",
            "id": "heading",
            "label": "Heading",
            "default": "Fight the cold easily"
        },
        {
            "type": "richtext",
            "id": "description",
            "label": "Description",
            "default": "<p>In the cold winter, VENUSTAS is your best partner! It heats up quickly and stays warm during outdoor activities, making it perfect for daily commuting or casual outings.</p>"
        } 
    ],
    "blocks": [
        {
            "type": "image",
            "name": "Image",
            "settings": [
                {
                    "type": "image_picker",
                    "id": "image",
                    "label": "Image"
                }   
            ]
        }
    ],
    "presets": [
        {
            "name": "Centered Image Slider"
        }
    ]
}
{% endschema %}